const userRepo = require("./userRepo");
const conversationRepo = require("./conversationRepo");
const tokenRepo = require("./tokenRepo");

module.exports = {
  userRepo,
  conversationRepo,
  tokenRepo,
};
